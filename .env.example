# Optional: background track URL for production (HTTPS). If unset, the app uses /14-days.mp3 from public/ — that file must be committed and pushed.
# VITE_BG_AUDIO_URL=https://your-cdn.example.com/14-days.mp3
